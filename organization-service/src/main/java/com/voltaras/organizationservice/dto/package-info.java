/**
 * Data Transfer Objects for the Organization Service.
 * <p>
 * Grouped into {@code request} and {@code response} sub-packages. DTOs are
 * never the JPA entities; mappers convert between the two.
 */
package com.voltaras.organizationservice.dto;
