/**
 * REST API controllers for the VOLTARAS backend.
 * <p>
 * This package contains Spring MVC {@code @RestController} classes that expose
 * HTTP endpoints. Each controller handles incoming requests, delegates business
 * logic to the {@link com.voltaras.backend.service service} layer, and returns
 * appropriate HTTP responses wrapped in {@link com.voltaras.backend.dto.response response DTOs}.
 * </p>
 */
package com.voltaras.backend.controller;
