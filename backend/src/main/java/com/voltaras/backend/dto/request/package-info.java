/**
 * Request DTOs for incoming API payloads.
 * <p>
 * This package contains DTO classes that model the structure of incoming
 * HTTP request bodies. These classes often carry Jakarta Bean Validation
 * annotations ({@code @NotBlank}, {@code @Email}, etc.) to enforce input
 * validation at the controller boundary.
 * </p>
 */
package com.voltaras.backend.dto.request;
