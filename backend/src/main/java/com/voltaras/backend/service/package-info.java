/**
 * Business logic service layer.
 * <p>
 * This package contains {@code @Service} annotated classes that implement
 * the application's business logic. Services orchestrate operations between
 * repositories and external systems, enforce business rules, and handle
 * transactional boundaries. Controllers delegate to services rather than
 * implementing logic themselves.
 * </p>
 */
package com.voltaras.backend.service;
