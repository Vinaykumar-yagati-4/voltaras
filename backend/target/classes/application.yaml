
server:
  port: 8084
spring:
  application:
    name: voltaras
  datasource:
    url: 'jdbc:mysql://localhost:3306/voltaras_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC'
    username: root
    password: Vinay@444
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace